import React from 'react';

const Page = () => {
    return (
        <div className={'h-screen w-full flex flex-col items-center justify-center'}>
            <span>Any article for the moment!</span>
        </div>
    );
};

export default Page;